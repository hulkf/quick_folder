# Quick Folder 项目说明与协作约定

## 项目作用

Quick Folder 是一个面向 Windows 的 PyQt5 桌面效率工具，主要用于集中管理常用文件夹、文件和程序入口，并提供常见的批量文件整理能力。

当前主要功能包括：

- 保存并快速打开常用文件夹，对文件夹执行分类、创建子目录、名称前缀处理等操作。
- 合并或提取文件，减少日常整理素材时的重复操作。
- 维护常用文件、程序和脚本的启动入口，并显示运行状态。
- 监控剪贴板文本，并按配置同步到飞书多维表格。
- 保存主题、窗口位置、文件夹操作顺序、启动项和飞书同步目标等本地配置。

主要入口和文件：

- `main_pyqt5.py`：应用主入口和主要界面逻辑。
- `run.bat`：Windows 下的直接启动入口。
- `config.json`：本地运行配置和用户设置。
- `feishu_sync.py`：飞书多维表格同步逻辑。
- `test_feishu_sync.py`：飞书同步相关测试。

## 修改完成后必须同步 GitHub

每次对本项目完成代码、配置、测试或文档修改后，都必须把已经验证的修改同步到 GitHub，不要只停留在本地。

完成一次修改时按以下顺序执行：

1. 检查 `git status` 和 `git diff`，确认本次修改范围。
2. 运行与修改内容匹配的检查或测试，不能把未经验证的结果标记为完成。
3. 只暂存本次任务相关文件，不要顺带提交用户已有的其他改动。
4. 创建内容明确、范围单一的 Git commit。
5. 推送当前分支到 GitHub 对应远程分支。
6. 再次检查本地分支与远程分支状态，并在交付说明中写明测试、commit 和 push 结果。

如果因为网络、权限、认证、合并冲突或远程拒绝而无法推送，必须明确说明阻塞原因和当前 commit 状态，不能声称已经同步完成。凭据、令牌、Cookie、个人路径、运行日志、缓存和临时产物不得提交到 GitHub。

## 修改原则

- 默认使用中文记录和交付。
- 修改前先确认真实运行入口和受影响范围，保留现有行为，避免无关重构。
- 工作区存在其他未提交改动时，视为用户正在进行的工作；不得覆盖、删除或混入当前 commit。
- 涉及界面或运行时问题时，要验证用户实际使用的 `run.bat` / `main_pyqt5.py` 路径，而不只是静态检查代码。
- 修改配置结构时要考虑旧版 `config.json` 的兼容性，避免升级后无法启动或丢失用户设置。
- 新增依赖、外部服务或飞书能力时，要同时记录安装、认证、权限和失败提示。

## 踩坑与经验记录

后续遇到项目特有的问题、容易重复犯的错误、环境限制或重要排查结论，都继续追加到本节。不要只把结论留在聊天记录里。

建议每条记录包含：

- 日期和问题现象。
- 根因及受影响范围。
- 最终解决办法。
- 验证方式。
- 如仍有风险，写明限制或回滚方法。

### 已知注意事项

- `config.json` 包含本机使用习惯和运行配置。修改默认值或字段结构时，需要兼容已有配置，且提交前应检查是否混入不适合公开的个人信息。
- 飞书同步依赖本机可用的 `lark-cli` 身份、目标多维表格权限以及正确的表和字段配置；同步失败时应分别检查本地认证、文档授权和字段类型。
- 仓库可能同时存在用户尚未提交的开发内容。执行同步时必须按任务精确暂存文件，禁止直接使用会把所有改动一并纳入的宽泛提交方式。
